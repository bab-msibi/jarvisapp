{
  "name": "jarvisapp",
  "version": "1.0.0",
  "private": true,
  "description": "Jarvis PWA — local dashboard and API bridge for OpenClaw",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
